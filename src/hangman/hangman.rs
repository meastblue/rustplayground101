pub struct Hangman {
    state: String,
    letters: Vec<String>,
    found_letters: Vec<String>,
    used_letters: Vec<String>,
    turns_left: usize,
}

impl Hangman {
    pub fn new() {
        
    }
}
