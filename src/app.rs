use crate::dictionary;
use crate::hangman;
pub struct App;

impl App {
    pub fn new() {
        Self::run();
    }

    fn run() {
        // hangman::display::Display::draw_welcome();
        // println!("Welcome to hangman game");
        // let word = dictionary::dictionary::Dictionary::new()?;
        // hangman::guess::Guess::read_guess();
        // Ok(())
    }
}
